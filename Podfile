# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'fastlane-jenkins' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for fastlane-jenkins

  target 'fastlane-jenkinsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'fastlane-jenkinsUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
